.container
  if user
    .row
      .col-md-12
        .well
          h1 User Logged in!
          if user.username
            h3 Hello: #{user.username}
          p.small User Id: #{user.id}
          p.small Email: #{user.email}
  block content
